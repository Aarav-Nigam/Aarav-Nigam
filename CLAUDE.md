# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository

This is the GitHub profile README repo (`Aarav-Nigam/Aarav-Nigam`). The only file that matters is `README.md` — it renders automatically on the GitHub profile page.

## Git / Push

The remote uses the personal SSH host alias, not the default `github.com`:

```bash
git remote set-url origin git@github-nigam:Aarav-Nigam/Aarav-Nigam.git
git push origin main
```

## README Conventions

- **Theme:** Tokyo Night across all GitHub stats widgets
- **Badges:** `shields.io` flat-square style
- **About Me:** Keep Swiggy work description intentionally vague — say things like "making search super awesome" rather than mentioning specific models, migrations, or internal tooling
- **Assets:** `xero-code.gif` lives in the repo root and is referenced via the raw GitHub URL; keep it in the README
- **Portfolio link:** `https://aarav-portfolio-2k25.netlify.app/`
